const int FIELD_WIDTH = 10;

const int FIELD_HEIGHT = 20;

const int GAME_TICK = 100;

const Duration GAME_DURATION = Duration(milliseconds: GAME_TICK);

const int DOWN_SPEED = 500;

const int LOCKDOWN_MARGIN = 500000;
